name: 'ArkGate Architecture Check'
description: 'Block merges on architecture violations (cross-layer imports, intent references) with arkgate-check'
author: 'pedroknigge'
branding:
  icon: 'shield'
  color: 'blue'

inputs:
  root:
    description: 'Project root to check'
    default: '.'
  config:
    description: 'Path to ark.config.json (relative to root)'
    default: 'ark.config.json'
  strict-config:
    description: 'Fail on config-coverage warnings too'
    default: 'true'
  baseline:
    description: 'Baseline file of frozen violations (empty = disabled)'
    default: ''
  version:
    description: 'arkgate version to run'
    default: 'latest'
  github-token:
    description: 'Token used to comment violations on the PR (empty = no comment)'
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Run arkgate-check
      id: ark
      shell: bash
      env:
        INPUT_ROOT: ${{ inputs.root }}
        INPUT_CONFIG: ${{ inputs.config }}
        INPUT_STRICT: ${{ inputs.strict-config }}
        INPUT_BASELINE: ${{ inputs.baseline }}
        INPUT_VERSION: ${{ inputs.version }}
      run: |
        set +e
        ARGS=(--root "$INPUT_ROOT" --config "$INPUT_CONFIG")
        [ "$INPUT_STRICT" = "true" ] && ARGS+=(--strict-config)
        [ -n "$INPUT_BASELINE" ] && ARGS+=(--baseline "$INPUT_BASELINE")
        # Prefer arkgate-check; fall back to ark-check if an older tarball is pinned.
        npx --yes --package "arkgate@$INPUT_VERSION" arkgate-check "${ARGS[@]}" 2>&1 | tee arkgate-check-output.txt
        STATUS=${PIPESTATUS[0]}
        {
          echo "## ArkGate architecture check"
          echo ""
          echo '```'
          cat arkgate-check-output.txt
          echo '```'
        } >> "$GITHUB_STEP_SUMMARY"
        echo "status=$STATUS" >> "$GITHUB_OUTPUT"
        exit "$STATUS"

    - name: Comment violations on PR
      if: failure() && github.event_name == 'pull_request' && inputs.github-token != ''
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.github-token }}
        PR: ${{ github.event.pull_request.number }}
        REPO: ${{ github.repository }}
      run: |
        {
          echo "### ArkGate architecture check failed"
          echo ""
          echo '```'
          cat arkgate-check-output.txt
          echo '```'
          echo ""
          echo "_Layer rules live in \`ark.config.json\`. Docs: https://github.com/pedroknigge/arkgate_"
        } > arkgate-comment.md
        gh pr comment "$PR" --repo "$REPO" --body-file arkgate-comment.md
