# 3.9.0 publish runbook (residual)

**Package version in tree:** `arkgate@3.9.0` (prepared).  
**Published on npm as of this note:** `3.8.3` (`npm view arkgate version`).  
**Blocker:** `npm whoami` returned **401 Unauthorized** against `registry.npmjs.org` in the
implementation session — do not claim publish success until the commands below succeed.

Z09 / residual `RB-11` remain open regardless of npm publish.

## Preconditions

- [ ] Working tree clean on the intended release commit
- [ ] `package.json` / lock / `src/version.ts` / `server.json` all `3.9.0`
- [ ] CI green on the release SHA (GitHub Actions)
- [ ] `npm run release:npm -- --dry` succeeds locally
- [ ] Registry auth: `npm whoami` prints the publisher account (not 401)

## Commands (after auth)

```bash
# 1) Dry packaging / release script
npm run release:npm -- --dry

# 2) Signed tag + push
git tag -s v3.9.0 -m "arkgate v3.9.0"
git push origin HEAD
git push origin v3.9.0

# 3) GitHub Release notes
gh release create v3.9.0 --verify-tag \
  --title "arkgate v3.9.0" \
  --notes-file docs/releases/3.9.0.md

# 4) npm publish (prefer workflow if that is project convention)
gh workflow run publish-npm.yml -f tag=v3.9.0 -f dry_run=false
# or, if direct publish is authorized for this account:
# npm publish --access public

# 5) Verify
npm view arkgate@3.9.0 version
npm view arkgate@3.9.0 gitHead
npm view arkgate dist-tags.latest
gh release view v3.9.0

# 6) MCP registry
mcp-publisher validate server.json
mcp-publisher publish server.json
# Confirm: io.github.pedroknigge/arkgate @ 3.9.0 with isLatest=true
```

## After successful publish

1. Flip CONTRIBUTING “Current published release” to 3.9.0 (remove “prepared” wording).
2. Set `docs/releases/3.9.0.md` front matter: **Prepared** → **Released**, Status published, real tag/release URL.
3. README banner: unqualified “current stable” / remove “npm latest is still 3.8.3” hedge.
4. Migration guide: restore 3.9.0 as current stable pin language if desired.
5. Optionally delete or archive this residual runbook.

## Do not

- Force-push main or rewrite published tags
- Mark Z09 / RB-11 / C-028 residual closed
- Claim Cursor/Codex hard write in release marketing
